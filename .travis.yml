language: node_js

branches:
  only:
    - development

before_script:
  - npm install codeclimate-test-reporter

script:
  - grunt restore
  - grunt build

after_script:
  - codeclimate < test/coverage/lcov.info

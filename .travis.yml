language: node_js

branches:
  only:
    - development

script:
  - npm install --no-optional
  - npm install codeclimate-test-reporter
  - grunt restore
  - grunt build
  - codeclimate < test/coverage/lcov.info
